open powershell in your main folder

git add .

git commit -m "descriptive message that says what you changed"

git push


wait a few minutes until the website is rebuilt. use ctrl+shift+R to reload the page without cache to view changes.